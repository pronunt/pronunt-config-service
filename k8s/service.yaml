apiVersion: v1
kind: Service
metadata:
  name: pronunt-config-service
  namespace: prism
spec:
  selector:
    app: pronunt-config-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP

